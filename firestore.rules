rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ログインしているユーザーからのリクエストの場合のみ、読み書きを許可する
      allow read, write: if request.auth != null; 
    }
  }
}